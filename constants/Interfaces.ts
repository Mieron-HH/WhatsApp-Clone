export interface Theme {
	dark: boolean;
	colors: {
		primary: string;
		background: string;
		secondary: string;
		card: string;
		text: string;
		secondaryText: string;
		border: string;
		notification: string;
		tab: string;
		activeTab: string;
		activeTabTintColor: string;
		inputContainer: string;
	};
}
